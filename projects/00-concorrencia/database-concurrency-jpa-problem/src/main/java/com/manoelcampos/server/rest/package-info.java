/**
 * Contém as classes que representam os Web Services REST
 * que podem ser chamados remotamente por outra aplicação.
 * 
 * Tais Web Services dão acesso a recursos (no nosso caso estes recursos
 * são dados). As classes que implementam tais serviços são
 * normalmente chamadas de Resources (Recursos).
 * 
 * As classes que implementam os WS REST
 * também são conhecidas como Controllers REST
 * (do padrão MVC: Model-View-Controller).
 * 
 * @author Manoel Campos da Silva Filho
 */
package com.manoelcampos.server.rest;
